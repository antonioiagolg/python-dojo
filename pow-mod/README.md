# Pow mod

This is a simple challenge. I decided to create two variables for the results to explain
better what is the result of the pow method, depending on the number of parameters. Basically
pow(a,b,c) means:

$$
a^b \mod{c}
$$
