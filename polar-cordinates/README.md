# Polar coordinates

It is possible to solve this usinig only python built-in functions and libs. To find the radius:
```
abs(complex_number)
```
And to find the phase angle:
```
import cmath
cmath.phase(complex_number)
```
