# Average

This was quite simple, the challenge has a great guide about sets. I added a simple verification in case the number of elements is zero, to not break the
application with division by zero error.
